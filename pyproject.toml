[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "catengine"
version = "1.0.3"
description = "Your game engine"
requires-python = ">=3.8"

[tool.maturin]
bindings = "pyo3"
features = ["python"]

[tool.maturin.env]
PYO3_USE_ABI3_FORWARD_COMPATIBILITY = "1"